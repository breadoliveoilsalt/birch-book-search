// These are not all the relevant imports I will need.  To update.

import React from 'react'
import { expect } from 'chai'
import Enzyme, { shallow } from 'enzyme'
import Adapter from 'enzyme-adapter-react-16'

Enzyme.configure({ adapter: new Adapter() })


describe("BookRecordClass", function() {

  it("should throw an error if not initialized with an object", function(){

  })

  it("should return an instance of BookRecord", function() {

  })

  describe("a BookRecord instance", function() {
    it("should have properties for title, author, publisher, and linkToExternalInfo", function() {

    })
  })
})
